const canvas = d3.select('.canvas');

const svg = canvas.append('svg'); // get a new tag element inside the selector component

// append shapes to svg container

svg.append('rect');
svg.append('circle');
svg.append('line');